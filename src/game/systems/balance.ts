/**
 * Balance System - Re-exported from data files for backwards compatibility
 */

// Re-export PowerUpType from powerups.ts where it's actually defined
export { type PowerUpType, POWER_UPS } from '../data/powerups'
